# مشروع: النظام الموحد لإدارة مطبعة ووكالة عمر رزين

> هذا الملف مرجع تشغيلي مختصر يُقرأ في **كل جلسة عمل**. المرجع الكامل التفصيلي (25 قسمًا) موجود في
> `docs/system_specifications_v2.md` — ارجع له عند الحاجة لتفاصيل أي قسم، لكن القواعد هنا هي الأهم والأكثر تكرارًا.

---

## 1. نبذة عن المشروع

منصة موحدة لمطبعة ووكالة إعلانية متكاملة (8 أفراد حاليًا، بدون فريق تطوير داخلي مذكور). تجمع:
POS، CRM، المبيعات، الإنتاج والـ Workflows، المخازن، المالية، Call Center، Unified Inbox،
التسويق، خدمات الوكالة، والتحليلات الذكية — كلها فوق معمارية **Cloud / Online بالكامل**
(قاعدة بيانات سحابية واحدة مشتركة، بدون Offline Mode — قرار موثّق، راجع القسم 4).

**هذا ليس مشروع بناء من الصفر.** فيه نظام ومنطق تجاري معتمد حاليًا (خصوصًا Pricing Engine
وحساب الدفاتر/الورق/التراج/الهالك)، والمطلوب توسيعه لا استبداله.

---

## 2. القواعد غير القابلة للتفاوض

هذه القواعد **ملزمة في كل تعديل أو Feature جديدة**، ولازم تُحترم حتى لو الطلب المباشر من المستخدم
ما ذكرهاش صراحة:

1. النظام الحالي هو المرجع في أي Business Logic معتمد بالفعل — عند التعارض، الأولوية له.
2. لا حذف لأي Feature موجودة إلا بطلب صريح.
3. لا تغيير في Pricing Logic بدون موافقة صريحة.
4. لا تغيير في طريقة حساب الدفاتر/الورق/التراج/الهالك بدون موافقة صريحة.
5. لا إنشاء Duplicate Logic إذا كان هناك Engine/Service موجود بالفعل — **دوّر قبل ما تبني**.
6. أي Feature جديدة لازم تتكامل مع الـ Architecture الحالية، مش تبني بمعزل عنها.
7. كل عملية مالية حساسة لازم تكون Audit Logged.
8. كل Production Job لازم يكون قابل للتتبع بالكامل.
9. كل Order لازم يمتلك History.
10. أي تغيير في قاعدة البيانات يتم عبر Migration — لا تعديل مباشر على الـ Schema.
11. لا تعديل على Legacy Logic إلا بعد Impact Analysis موثّق.
12. لا استبدال لأي Architecture قائمة دون توثيق السبب.
13. أي Integration خارجي (WhatsApp, Social, ...) لازم يكون قابلًا للفصل عن الـ Core System.
14. الـ Workflow لازم يكون Dynamic وقابل للتوسع — ممنوع Hardcode لخطوات الإنتاج.
15. الـ Thresholds وBusiness Settings لازم تكون قابلة للإدارة، مش Hardcoded في الكود.
16. النظام يعمل Online بالكامل — لا يوجد Offline Mode مطلوب في النسخة الحالية.
17. انقطاع الإنترنت المؤقت لازم يتعامل معه بشكل رشيق (تنبيه واضح، عدم فقدان بيانات نموذج مفتوح) — مش Sync كامل.
18. صلاحيات المستخدمين (RBAC) تحدد مين يشوف/يعدّل كل Module — تُبنى مع كل موديول من أول يوم، مش تُضاف لاحقًا.
19. أي عملية Delete حساسة يُفضّل تكون Soft Delete + Audit Trail.
20. أي تغيير معماري كبير يحتاج Impact Analysis واختبار للوظائف المتأثرة قبل التنفيذ.

---

## 3. قبل إضافة أي Feature — Checklist إلزامي

- [ ] هل قريت الكود/الـ Service الموجود المرتبط بالـ Feature دي؟
- [ ] هل فيه Engine أو Service موجود فعلاً بيعمل جزء من المطلوب؟ (لو أيوه، وسّعه، ماتكررهوش)
- [ ] هل الـ Feature دي بتلمس Pricing Engine أو حساب الدفاتر/الهالك؟ لو أيوه → **وقف واطلب موافقة صريحة**
- [ ] هل الـ Feature محتاجة Migration لقاعدة البيانات؟
- [ ] هل الـ Feature بتحتاج صلاحيات RBAC جديدة؟
- [ ] هل الـ Feature بتتعامل بشكل رشيق مع انقطاع إنترنت مؤقت (رسالة واضحة، عدم فقدان بيانات نموذج مفتوح)؟
- [ ] هل العمليات الحساسة فيها (مالية/حذف/تعديل سعر) هتتسجل في Audit Log؟

اختبار الاكتمال (قسم 23 بالمرجع الكامل): Happy Path + Validation + Error Handling +
Permissions + DB Persistence + مقاومة انقطاع الاتصال المؤقت + Audit Log + Regression على القديم.

---

## 4. البنية المعمارية الأساسية

```
Customer → Order → Order Items → Production Jobs → Production Steps
```

- أوردر واحد للعميل ممكن يحتوي منتجات متعددة (دفتر + كارت + فلاير)، كل منتج = Job مستقل بـ Workflow خاص بيه.
- مثال Workflow دفتر أوفست: `Design → Pre-Press → Plate → Printing → Numbering → Folding → Binding → Packaging`
- مثال Workflow كارت ديجيتال: `Design → Digital Printing → Cutting → Packaging`
- رحلة العميل الكاملة المطلوب تتبعها من مكان واحد:
  `Marketing/Social/Call Center → Lead → Customer → Quotation → Order → Production Job → Workflow → Inventory → Finishing → Packaging → Invoice/Payment → Delivery → Follow-up → Re-Order`

### محطات العمل (Cloud — قسم 4 بالمرجع الكامل)
كلها Clients بتدخل على نفس قاعدة البيانات السحابية الواحدة، الفرق بينها دور/صلاحية (RBAC) بس:

| المحطة | المسؤولية |
|---|---|
| Front Office/POS | عملاء، أوردرات، عروض أسعار، فواتير — فتح أمر الشغل من على النظام مباشرة (بدون QR) |
| Pre-Press | ملفات التصميم، مونتاج، فصل ألوان، تحويل الحالة لـ "جاهز للطباعة" |
| Admin | الإدارة العليا، المراقبة، التقارير، التطوير والصيانة |

النظام يحتاج اتصال إنترنت مستمر (لا يوجد Offline Mode). النسخ الاحتياطي مسؤولية الاستضافة
السحابية (Supabase) — تأكد إن الـ Automated Backups مفعّلة على الـ Plan المستخدم.

### أمر الشغل (Job Card) — بدون QR (قسم 6.4 بالمرجع الكامل)

**قرار موثّق:** تم إلغاء طباعة أمر الشغل ومسح QR بالكامل. كل Job بيتفتح ويتحدّث مباشرة من شاشة
النظام (بحث برقمه، أو ضغطة من Production Queue أو الشاشة الموحدة) — الـ Job ID نفسه هو مفتاح
التنقل، بدون أي كود مطبوع أو ممسوح.

### الشاشة الموحدة لمتابعة الإنتاج (قسم 6.5 بالمرجع الكامل)

شاشة واحدة تعرض حالة كل الأقسام الإنتاجية لحظيًا (Jobs جارية، متأخرة، حالة الماكينات، أعلى أولوية).
**قاعدة إلزامية:** الضغط على أي Job فيها لازم يفتح مباشرة شاشة القسم/الماكينة المسؤولة عن مرحلته
الحالية (نفس Job ID المستخدم في أمر الشغل) — دي نقطة دخول (Aggregation Layer) مش شاشة تفاصيل
مستقلة، وممنوع تتخزن فيها بيانات إضافية موازية لتفادي Duplicate Logic (قاعدة 5).

> **ملاحظة تنفيذ حالية (محدَّثة 2026-09-08، تكملة 64):** "لوحة الإنتاج" (Production Board) بقت
> تغطي الوصف ده بالكامل تقريبًا — تابات فرعية لكل مسار إنتاجي + تاب "الكل" موحّد يجمع كل
> الأقسام/المسارات مع بعض في نفس اللحظة (فلتر مسار من جوّه، ترتيب Drag-and-Drop حقيقي خاص بكل
> مرحلة، عمود اسم الصنف) + تاب "حسب الوركفلو" (فيو Kanban حقيقي — أعمدة = مراحل وركفلو مختار)،
> وكل صف بيودّي لتفاصيل الـWork Order بتاعه. الوحيد الناقص فعليًا دلوقتي: حالة الماكينات (مفيش
> Machine/Capacity model خالص، مؤجل لدفعة تانية).

### نوعين من الـ Jobs — Production Job وProcurement Job (قسم 12.4 بالمرجع الكامل)

مش كل Order Item بيتصنّع داخليًا. المنتجات الجاهزة (أدوات مكتبية، كتب خارجية...) بتاخد مسار
مختلف تمامًا: **Procurement Job** بدل Production Job — `Purchase Request → Sent to Supplier →
Delivery أو Pickup (يحدد لكل مورد/طلب) → Received → Ready`. النظام يحدد نوع الـ Job تلقائيًا
حسب إعداد المنتج (مُصنَّع داخليًا / يُشترى جاهز)، من غير تدخل يدوي وقت الأوردر.

**✅ خلص (2026-08-26، تكملة 40 في `docs/AI/PROJECT_STATUS.md`).** كان مؤجل عمدًا، owner أكّد
البدء فيه صراحة. `ReadyProduct.sourceType` كان موجود من زمان بس "metadata only" — دلوقتي بند
"منتجات جاهزة" بيوجّه كل منتج لمسار `OTHER_PRODUCTS` (داخلي، تصميم←إنتاج←تسليم) لو
`sourceType: INTERNAL_PRODUCTION`، أو `READY_PRODUCTS` (Procurement، فيه مرحلة مورد خارجي)
غير كده — توجيه لكل بند لوحده مش لكل تاب ثابت، صفر تغيير Backend. زائد متغيّر مرحلة "طريقة
الاستلام" (توصيل/استلام) على مرحلة "الإحضار من المورد".

### Service Catalog للوكالة الإبداعية (قسم 17 بالمرجع الكامل)

خدمات الوكالة (تصميم، مونتاج، تصوير، تسويق، مواقع) مقسّمة لأنواع فرعية محددة بالاسم (مثلاً:
هوية بصرية، فيديو ريلز، تصوير منتجات، إعلانات ممولة، متجر إلكتروني...). القائمة دي **قابلة
للإضافة من الإدارة** وليست Hardcoded — أي Feature إدارة للخدمات لازم تدعم إضافة/تعديل نوع خدمة
جديد بدون تعديل كود (قاعدة 15).

**✅ تصحيح توثيقي (2026-08-26، تكملة 41 في `docs/AI/PROJECT_STATUS.md`) — كان مكتوب هنا "مؤجل
لدفعة تانية"، اتضح بالفحص إنه موجود ومكتمل بالفعل.** 30 خدمة فرعية مسمّاة حقيقية موجودة على
القاعدة الحية (`هوية بصرية / براندنج`، `إعلانات ممولة (Meta/Google/TikTok)`، `متاجر إلكترونية`،
`تصوير منتجات`... إلخ) موزّعة على الأقسام الخمسة، وشاشة `/settings/services` (`ServicesEditor.tsx`)
بالفعل تدعم إضافة/تعديل/حذف أي خدمة بدون أي تعديل كود. الوحيد الناقص فعليًا: كل الأسعار لسه 0.00
(بيانات محتاجة إدخال من owner، مش فجوة كود).

### معادلة تكلفة الديجيتال — نظام التنزيلة (Yield) (قسم 13.3 بالمرجع الكامل)

الماكينة بتاخد ربع فرخ بس كحد أقصى، لكن مقاس القطعة بيختلف من طلب لآخر — فمعادلة "÷4 ثابتة"
غلط. المتغير الأساسي هو **Yield = عدد القطع اللي بتتنزل على الربع الواحد** (النظام يقترحها
تلقائيًا من المقاس، وPre-Press يعدّلها قبل التسعير النهائي):

```
لو القطعة ≤ الربع:  تكلفة الورق = (سعر الفرخ ÷ 4) ÷ Yield
لو القطعة > الربع:  تكلفة الورق = (سعر الفرخ ÷ 4) × Units Needed (عدد الأرباع اللازمة)
```

نفس المنطق يتطبق على تكلفة الطباعة والسلوفان (بيتغطوا الربع كله). البشر يُحسب بالقطعة مباشرة.
هذه قاعدة تسعير موثّقة **من الآن فصاعدًا لا تُعدَّل** إلا بموافقة صريحة (قاعدة 3 في قسم 20).

**موافقة صريحة من صاحب المشروع على تنفيذها مأخوذة بالفعل** — مسار تسعير الديجيتال مش موجود في
الكود أصلاً (كان شاشة "قريبًا")، يعني ده أول تنفيذ فعلي له، مش تعديل على حساب معتمد سابقًا.
**جاري تنفيذها دلوقتي.**

---

## 5. خارطة التنفيذ بالمراحل (لا تبني كل حاجة دفعة واحدة)

| # | المرحلة | المحتوى |
|---|---|---|
| 0 | فهم النظام الحالي | توثيق الـ Architecture والـ Business Logic الموجودة فعلاً قبل أي كود جديد — ✅ مكتملة |
| 1 | الأساس التشغيلي | Customer, Order, Job أساسي, Inventory, Invoicing, Audit أساسي — ✅ مكتملة |
| 2 | محرك الإنتاج | Dynamic Workflow Engine, Production Queue/Dashboard, أمر شغل بدون QR, شاشة موحدة — 🔄 جارية |
| 3 | ~~Offline/Sync~~ | **ملغاة نهائيًا.** النظام سحابي Online بالكامل بقرار صاحب المشروع — راجع قسم 4 |
| 4 | CRM والتواصل | Customer 360°, Leads Pipeline, Unified Inbox (يبدأ بقناة واحدة رسمية), Call Center أساسي |
| 5 | التسويق والذكاء | Campaigns, Content Calendar, Auto Re-Order, Smart Decision Engine |

> **حالة المشروع الحالية (آخر تحديث: 2026-08-26):** المرحلة 2 جارية. **مرجع تفصيلي حي محدَّث
> باستمرار:** `docs/AI/PROJECT_STATUS.md` — راجعه أول أي جلسة جديدة، هو الأدق من السطر ده.
> **دفعة التنفيذ الثلاثية اللي كانت متفق عليها خلصت بالكامل** (الحضور/الانصراف، تكلفة
> الديجيتال Yield-Based، الشاشة الموحدة الحقيقية).
>
> **بعد كده اتشحن (تفاصيل كاملة كلها في `docs/AI/PROJECT_STATUS.md`):** "تصميم واحد بمتغيرات
> إنتاج متعددة" + موافقة العميل، تقفيل حساب الخزينة اليومي الحقيقي، تسعير الديجيتال المُعاد
> بالكامل (A4/A3 مباشر + شرائح كمية)، Leads Pipeline بسيط (موديول جديد منفصل عن العملاء)،
> ثم طلب owner الكبير "إصلاح دورة الفاتورة والدفعات والمرتجعات" — **خلص بالكامل دلوقتي** (بيع
> سريع لأكتر من صنف، تعديل/حذف الدفعات، مرتجعات بضاعة مخزون، تخفيض على صنف محدد، تقفيل حساب
> تلقائي بوقت محدد، وآخر بند: تعديل السعر يدويًا للوحات/منتجات جاهزة/خدمات/بضاعة المخزون —
> تفاصيل كل بند في `docs/AI/PROJECT_STATUS.md`).
>
> **أحدث دفعة (2026-08-24/25):** **نظام التحكم في الأجهزة المسموح لها بالدخول** (Device
> Access Control — ميزة أمان جديدة بالكامل: تسجيل تلقائي أو اعتماد يدوي قابل للتبديل، جهاز
> عام أو مربوط بموظف واحد، كشك الحضور مستثنى بالكامل) و**تصنيفات مخزون قابلة للإدارة** —
> تصنيف حر جديد (`InventoryCategory`) منفصل تمامًا عن حقل "الفئة" التقني
> (`MaterialCategory`، الـ6 قيم الثابتة المرتبطة بمحرك التسعير — **متلمسش**)، مع فلتر بحث،
> تصنيف فعلي لـ236 صنف حقيقي موجود، فلتر تصنيف على ويدجت "بضاعة ناقصة" (لتقارير المشتريات)،
> وقدرة تعديل "الفئة" التقنية نفسها بعد الإنشاء. زائد دفعة تصليحات UI حقيقية اكتشفها owner
> أثناء الاستخدام الفعلي (مقاس التكسير الحقيقي في أمر الشغل الأوفست، اسم المورد لكل صنف،
> بحث الموقع، زرار الطباعة، ومشكلة متكررة في اختيار القوائم المنسدلة).
>
> **دفعة تصحيحات Pricing Engine (2026-08-26)، كلها بموافقة owner صريحة (قاعدة 3)، تفاصيل كل
> بند في `docs/AI/PROJECT_STATUS.md` (تكملة 22-25):**
> 1. `numberingRunPriceOverride` (كان `numberingCostOverride`) بقى بيستبدل سعر التراج الواحد
>    للترقيم ويتضرب في عدد التراجات، مش يحل محل الناتج الكلي — زائد عرض سعر التجليد الكلي في
>    تفصيل تكلفة بند الدفاتر.
> 2. تعديل سعر يدوي (سعر الوحدة/المتر) للوحات وإعلانات/منتجات جاهزة/خدمات/بضاعة المخزون —
>    آخر بند من طلب owner الكبير "إصلاح دورة الفاتورة والدفعات والمرتجعات"، **الطلب ده خلص
>    بالكامل دلوقتي**. زائد وضع تاني بديل: نسبة % بدل السعر الثابت.
> 3. تعديل سعر الفرخ + إجمالي سعر الورق (تعديلين منفصلين ومستقلين) للورق السايب/الدفاتر/
>    الفولدرات — الأول يحل محل سعر الفرخ المرتبط بالمخزون، التاني يحل محل الناتج النهائي
>    مباشرة.
> 4. عرض "قبل نسبة الربح" جنب سعر البند (بدون تغيير في أي حساب) + اسم العميل في ملف PDF
>    المُصدَّر للفاتورة/عرض السعر.
> 5. خصم البند الواحد ("خصم على البند ده") بقى نسبة % بدل مبلغ ثابت بالجنيه — استبدال كامل
>    (owner صريح، مش وضعين مع بعض زي بند 2). العمود المُجمَّد في قاعدة البيانات فضل مبلغ عملة
>    زي ما هو (الفواتير التاريخية متأثرتش)، التحويل نسبة→مبلغ بيحصل سيرفر-سايد بس. زائد أول
>    خصم بيتطبق في "البيع السريع" من المخزون خالص (تكملة 32 في `docs/AI/PROJECT_STATUS.md`).
>
> بند "QR على أمر الشغل" اتلغى نهائيًا (قرار صاحب المشروع — كل التتبع من على النظام مباشرة).
>
> **محرك إشعارات فعلي بدأ (2026-08-26)، تفاصيل كل بند في `docs/AI/PROJECT_STATUS.md`
> (تكملة 27-29):** تاب "توقع إعادة الطلب" لكل عميل (متوسط الفترة بين الطلبات لكل صنف على
> حدة، مطابقة Best-effort لأن `OrderItem` مفيش له FK حقيقي للكتالوج بعد التجميد) + تعديل
> يدوي على الموعد المتوقع (معدل استهلاك يومي أو تاريخ مباشر، أولوية للتاريخ المباشر) + ويدجت
> داشبورد "عملاء قرّب ميعاد إعادة الطلب المتوقع بتاعهم" يجمع كل العملاء في مكان واحد + رابط
> واتساب مباشر (`wa.me`) قابل للضغط على رقم العميل + زرار رسالة تذكير جاهزة بالأصناف
> المستحقة. **الإرسال التلقائي الفعلي من السيستم لرقم العميل مباشرة لسه مؤجل عمدًا** — بعد
> بحث فعلي اتضح إن ده محتاج حساب WhatsApp Business Platform (Cloud API) رسمي موثّق مع Meta
> (مش WhatsApp Business العادي اللي عند owner دلوقتي) — **حظر خارجي حقيقي محتاج قرار/إعداد من
> owner، مش تقني.**
>
> المرحلة 4 (CRM/تواصل) لسه جزئية جدًا (Customer 360° + Leads Pipeline موجودين، بدون Inbox/
> Call Center)، والمرحلة 5 (تسويق/قرارات ذكية) لسه صفر. مؤجل لدفعة تانية:
> الباركود وقسم المكتبة اتضح إنه موجود ومكتمل بالفعل (تصحيح توثيقي، تكملة 31)، توسعة Service
> Catalog للوكالة، Machine/Capacity model، وباقي محرك الإشعارات (إرسال واتساب تلقائي فعلي —
> محتاج حساب رسمي أولاً).
>
> **🎉🎉 مبادرة "فصل الخزينة/الربح بالفرع + الموردين + التقارير" خلصت بالكامل بكل بنودها الـ8
> (2026-08-26)، تفاصيل كل جزء في `docs/AI/PROJECT_STATUS.md` § 6 وتكملة 33-39:** owner طلب
> 8 أجزاء منفصلة مرة واحدة (بعد تصريحه "متبنيش وخلاص البند اللي مش فاهمه إسألني علشان نبني
> مظبوط ومنقعدش نعدل ياما"). **كلها خلصت ومتحقق منها حي:** (1) سعر تكلفة قابل للمنح كصلاحية،
> (2) فصل خزينة الفرعين + صافي ربح لكل فرع + فلتر فرع في شاشة الخزينة، (3) صفحة موردين مستقلة
> بكشف حساب حقيقي، (4) Workflow موردين للوحات/الإعلانات بتكلفة مورد تلقائية من المقاس،
> (5) تاب كشف حساب عميل قابل للطباعة، (6) صفحة `/reports` شاملة جديدة (أرصدة لكل طريقة دفع +
> 7 تابات)، (7-8) عهدة نقدية حقيقية لموظف المبيعات — بتتزود من كل تحصيل CASH/INCOME وبتتصفر
> تلقائيًا مع تقفيل حساب الفرع (محسوبة وقت القراءة، صفر تخزين إضافي)، ويدجت داشبورد جديد
> "عهدتك النقدية الحالية". **مفيش بند متبقي من الطلب الأصلي.**
>
> **بعد كده (2026-08-27):** فصل شاشة "الأسعار الثابتة" لـ3 مجموعات (أوفست/لوحات وإعلانات/
> ديجيتال) + تكلفة حقيقية اختيارية للزنكات والورق (`Setting.zincSupplierCost`/
> `SheetType.costPrice`، منفصلة تمامًا عن معادلة التسعير — تفاصيل في قسم 9)، و`resolveItemProfit`
> بقى يحسب ربح الأوفست الحقيقي لما البيانات متاحة (تكملة 42 في `docs/AI/PROJECT_STATUS.md`).
> تصحيح فوري بعدها: تسمية حقل تكلفة الزنكات كانت بتسمّي المورد الحالي بالاسم — **قاعدة عامة
> جديدة: ممنوع تسمية مورد حقيقي بعينه في أي Label دائم بالواجهة، النظام يوصف المفهوم
> ("المورد الحالي") مش كيان بعينه**، لأن العلاقة مع المورد ممكن تتغيّر.
>
> **وبعد كده:** كتالوج جديد لأصناف اللوحات والإعلانات بسعر ثابت لا تتبع معادلة المادة/المقاس
> (زي "روول أب" — حامل + بانر)، موديل `BoardsCatalogItem` جديد (سعرنا + تكلفة مورد اختيارية
> خلف صلاحية `inventory.costPrice`)، مسار تسعير جديد داخل `kind: 'BOARDS'` نفسه
> (`boardsCatalogItemId` على البند يتخطى المعادلة بالكامل، نفس نمط `readyProductId`/
> `serviceId`)، زيرو كود جديد لإعادة استخدام Workflow المورّد الخارجي وتقرير الربح الحقيقي —
> شاشة إعدادات CRUD + زرار تبديل "خامة ومقاس"/"من الكتالوج" في كومبوزر الطلب. متحقق منه
> بالكامل حي. تفاصيل في تكملة 43.
>
> **وبعد كده، 🎉 مبادرة "ربط كل صنف بموردة" خلصت بكل أجزائها الأربعة (owner: "لازم المنتجات
> اللي في المخزن أقدر اربطها بالمورد"):** (1) `InventoryItem.supplierId` — مورد افتراضي لكل
> صنف مخزون (ورق، ختم، ...)، عمود جديد قابل للتعديل في شاشة المخزن مباشرة؛ (2) "قائمة شراء
> عاجل" — تاب Actionable جديد جوة شاشة المخزن، موديل `PurchaseRequest` بيتسجل تلقائي
> سيرفر-سايد لما أوردر حقيقي (مش مسح دوري) يخصم مخزون تحت الصفر، و"اتشرت" بتضيف الكمية
> للمخزون (حركة `IN`) وتسجل المبلغ على حساب المورد (`SupplierPurchase`) مع بعض في نفس الوقت؛
> (3) حقلين مورد منفصلين على `BoardsCatalogItem` (`purchaseSupplierId`/`assemblySupplierId` —
> شراء الروول أب + تركيب البانر عند جهة تانية)، `PurchaseRequest.kind` جديد
> (`BOARDS_PURCHASE`/`BOARDS_ASSEMBLY`) بيتحط له صفّين تلقائيًا في نفس القائمة كل مرة يتطلب
> الصنف — بدون أي تتبع مخزون خالص؛ (4) **تصحيح توثيقي — كان مكتمل بالفعل من عمل سابق**: كشف
> حساب المورد (`SupplierDetailPage.tsx`) بيجيب كل عمليات الشراء من غير فلتر على المصدر، فأي
> "اتشرت" من الجزء 2/3 بيظهر تلقائيًا في كشف الحساب والرصيد بصفر كود إضافي — owner أكّد إن
> رصيد إجمالي بس يكفيه. **الأربعة متحقق منهم بسكريبت تكامل مباشر حي، صفر بيانات وهمية باقية.**
> ملاحظة وحيدة: الجزء 3 — شاشة الإعدادات اتفتحت وعرضت الحقلين صح، لكن ضغطة الماوس اليدوية
> على Combobox اختيار المورد واجهت مشكلة تقنية في أداة المتصفح الآلي نفسها (مش عيب كود).
> **مفيش بند متبقي من طلب owner الأصلي.** تفاصيل كل جزء في تكملة 44-46.
>
> **وبعد كده:** الدفاتر بقى فيها خيار "وجه واحد/وجهين" في الطباعة — كان ناقص خالص من معادلة
> الدفاتر (موجود من زمان في الورق السايب/الفولدر بس)، owner اكتشفه وهو بيحسب شغلانة حقيقية.
> بموافقته الصريحة (بيلمس معادلة التسعير، قاعدة 3): "وجهين" بتضاعف عدد التراجات بس، نفس
> معادلة الورق السايب بالحرف، خانة واحدة مشتركة للدفتر كله (زي `colorCount`). متحقق منه
> بوحدة اختبار جديدة واختبار حي على المتصفح. تفاصيل في تكملة 47.
>
> **وبعد كده:** "شكل مختلف لكل وجه" — امتداد لـ"وجهين" أعلاه، بموافقة owner صريحة وإثبات رياضي
> قبل الكود إنها توسعة صافية صفر تراجع: كل وجه بقى ليه عدد ألوان وتصميم مستقلين اختياريين (ورق
> سايب/دفاتر/فولدرات)، عدد التراجات = مجموع ألوان الوجهين بدل الضرب. زائد بحث مقاس موحّد جديد في
> كومبوزر الطلب (اكتب المقاس، النظام يحدد مجموعته تلقائيًا — بدل الدوران بين المجموعات يدويًا).
> ثم إصلاح رسالة "Validation failed" العامة اللي كانت مش بتحدد أي بند فيه المشكلة — بقت تعرض
> مكان الخطأ بالظبط (`items[4].pricing.colorCount`) بالعربي، تعديل واحد في `errorHandler.ts`
> بيوصل لكل شاشة Save في النظام. زائد رفع حد طول "اسم البند" من 50 لـ300 حرف (owner لقى الفجوة
> بفضل رسالة الخطأ الجديدة). تفاصيل في تكملة 48-50.
>
> **🔴 وبعد كده، إصلاح حرج:** كل التعديلات اليدوية على التسعير (نسبة الربح، أسعار الزنكة/التراج/
> التصميم، خصم البند، ريزة/جراب/فورمة/تكسير الفولدر، تعديل سعر الوحدة/المتر...) كانت بتتفقد
> بصمت لما owner يعدّل **أي بند تاني** في نفس الفاتورة/عرض السعر ويحفظ — owner: "بيتضاف عليها
> النسبة المثبتة تاني وبيلغي النسبة اللي انا كنت عاملها". السبب: القيمة الخام للتعديل كانت
> بتتغذى للمحرك مرة واحدة وقت أول حفظ لكن متكنش بتتجمّد في الـbreakdown — وبما إن تعديل مستند
> بيستبدل كل البنود مرة واحدة، أي بند لم يُلمس كان بيرجع لإعدادات النظام الافتراضية تلقائيًا.
> إصلاح شامل في `pricingEngineService.ts` (تجميد القيم الخام) و`NewOrderPage.tsx` (استرجاعها)
> يغطي كل الأنواع. متحقق منه حي بسيناريو طابق شكوى owner بالحرف — التعديلات فضلت زي ما هي بعد
> دورة حفظ→تعديل بند تاني→حفظ→إعادة فتح كاملة. تفاصيل في تكملة 51.
>
> **وبعد كده:** دفاتر — كل "نسخة" (`COPY_n`) بقت تقدر تاخد طباعة مستقلة تمامًا عن باقي الدفتر
> (عدد ألوان، وجه واحد/وجهين، شكل مختلف لكل وجه، تصميم جديد) — owner واجه شغلانة حقيقية (الأصل
> مكوّن من ورقتين مختلفتي الطباعة، مش أصل+كربون تقليدي). بموافقته الصريحة "ابني الميزة دي فعليًا"
> وإثبات رياضي قبل الكود إن نسخة من غير طباعة مستقلة مفعّلة بتفضل جزء من نفس الحساب القديم
> بالحرف (صفر تراجع). متحقق منه حي بأرقام owner الفعلية طابقت الحساب اليدوي 100%. تفاصيل في
> تكملة 52.
>
> **وبعد كده، 3 تصحيحات في مستند عرض السعر المطبوع/PDF:** اسم ملف الـPDF بقى "عرض سعر [اسم
> العميل]" بدون رقم العرض؛ "الإجمالي قبل الخصم" بقى يظهر بس لو فيه خصم فعلي (كان بيظهر دايمًا
> برقم قبل التقريب، حتى لو مفيش خصم، فكان بيوهم بخصم وهمي)؛ 🔴 وإصلاح جوهري في مصدّر الـPDF —
> كان بيقطع صفوف الجدول نصين على حدود الصفحات وصفحة 2 كانت بتبدأ من غير هيدر خالص، بقى يقيس
> مكان كل صف حقيقي قبل التصوير ويغيّر نقطة القطع لأقرب حد صف، ويكرر الهيدر/اللوجو/صف عناوين
> الجدول في أول كل صفحة تانية. متحقق منه بمحاكاة رياضية بأرقام حقيقية (صفر انتهاكات) + تصدير
> PDF حي فعلي. تفاصيل في تكملة 53.
>
> **وبعد كده، تكملة تصدير PDF على عدة جولات حقيقية متتالية مع owner (نفس ملف الـ13 بند) لغاية
> ما التصميم استقر:** الختم والجملة اللي تحتها + بيانات التواصل بقوا يتكرروا مع بعض في آخر كل
> صفحة (مثبَّتين على الهامش السفلي بالظبط أيًا كان طول المحتوى، مش طايفين فوقه في المستندات
> القصيرة)، هيدر الصفحات التالية بقى "مختصر" (لوجو + اسم بس)، هامش طباعة 1 سم فاضي على الأربع
> جهات، و🔴 خوارزمية التقسيم اتعملها إعادة كتابة كاملة بعد ما owner لقى عيب حقيقي (بند واحد
> بيقع لوحده في صفحة والي بعده في صفحة تالتة) — التصميم الجديد بيعبي كل الصفحات للأمام بكثافة
> قصوى الأول، وبعدين يدمج الصفحة الأخيرة في اللي قبلها بس لو طلعت من غير ولا بند حقيقي واحد.
> **ثم تصحيح نطاق حاسم:** owner وضّح إن كل ده كان المفروض يتطبق على عرض السعر بس، مش الفاتورة
> ("بس انا مطلبتش تعدل ده في الفاتورة") — رغم إن الملف مشترك بين الاتنين. `downloadDocumentAsPdf`
> بقى بياخد `{ enhanced?: boolean }`: الفاتورة/أمر الشغل/كشوف الحساب/التقارير كلهم فاضلين على
> السلوك الأساسي (منع قطع الصفوف بس، صفر هامش)، وعرض السعر بس (`QuotationDocumentPage.tsx`)
> بيمرر `{ enhanced: true }` عشان ياخد الهامش/تكرار الختم/الهيدر المختصر/ضمان بند في آخر صفحة.
> متحقق منه حي بدقة البايت على ملفي PDF حقيقيين فعليًا (فك تشفير الـcontent streams). تفاصيل
> كل بند في تكملة 54.
>
> **وبعد كده:** عنوان مستند الفاتورة المطبوع بقى قابل للتخصيص من الإعدادات (owner: "عايز
> الفاتورة... أقدر انا أغير إسمها... أكتب إنها فاتورة أو إذن إستلام او أي حاجه بمزاجي") —
> `Setting.invoiceDocumentLabel` نص اختياري، فاضي = الافتراضي "فاتورة" زي ما هو، عرض السعر/أمر
> الشغل مالهمش نفس الطلب فمتلمسوش. تفاصيل في تكملة 55.
>
> **وبعد كده:** كشف حساب العميل بقى فيه تصفية بأصناف معينة (owner: "عايز يكون عند العميل كشف
> حساب اقدر اطبعه لأصناف بعينها من كل الأصناف اللي العميل طلبها") — لوحة اختيار بأسماء كل
> الأصناف اللي طلبها العميل تاريخيًا، اختيار صنف أو أكتر بيفلتر كل فاتورة على الأصناف المطابقة
> بس ويشيل أي فاتورة معندهاش ولا صنف منهم، مع كارت منفصل "إجمالي الأصناف المختارة فقط" (بدون
> أي لمس لإجماليات الفاتورة المالية نفسها). تفاصيل في تكملة 56.
>
> **وبعد كده، 🐛 إصلاح:** زرار "⧉ كرر بمقاس أو كمية مختلفة" جنب كل بند في السلة (تكرار بند
> وتعديله، زي تغيير الاسم لبند نفس الحساب بالظبط) كان شغال فعليًا وبيملي الكومبوزر صح، بس
> **فوق الصفحة برّه الشاشة الحالية من غير أي Scroll** — لو المستخدم نازل تحت في فورم طويل أو
> السلة تحت الفورم، كان بيبان إنه "مش بيعمل حاجة خالص" (owner: "بدوس عليه مش بيعمل حاجه خالص").
> الإصلاح: `scrollComposerIntoView()` بيودّي الصفحة تلقائيًا لمكان الكومبوزر عند التكرار أو
> التعديل، متحقق منه حي بنفس السيناريو اللي وصفه owner بالحرف. تفاصيل في تكملة 57.
>
> **وبعد كده، ميزة كبيرة جديدة للمرتبات** (owner: "لما الشهر يخلص يتحسب المرتب بالظبط ويتحفظ
> لحد ما يتصرف للموظف والشهر الجديد يكون منفصل عن القديم علشان منخلطش بين الشهور") — موديل
> `PayrollPeriod` جديد بيتقفل تلقائيًا لوحده (بدون زرار، owner اختار كده صراحة) لحظة ما دورة
> صرف الموظف الشهري تخلص فعليًا (Sweep كل دقيقة، نفس شكل `autoCloseDayJob.ts`)، بيجمّد الراتب/
> معدل الساعة/تفصيل كل يوم/الإجمالي المستحق بالظبط وقت القفل. "صرف مرتب" بقى بيصرف من الفترة
> المقفولة دي لو موجودة (بدل الحساب الحي القديم اللي كان بيضيع أول ما الفترة تتغير) — صفر
> تراجع لموظفي المرتب الأسبوعي أو لحد لسه شهره ما خلصش. زرار "إعادة فتح" (SUPER_ADMIN/ADMIN،
> owner اختار السماح بيه صراحة لحالة اكتشاف خطأ حضور بعد القفل) بيشتغل بس لو الفترة معندهاش
> صرف مسجّل عليها، وبيسيب نفس الـSweep التلقائي يعيد الحساب ويقفلها تاني — صفر زرار "إعادة
> حساب" منفصل. متحقق منه حي بالكامل: قفل تلقائي حقيقي لـ3 موظفين، صرف مرتب حقيقي نجح وربط
> صح، إعادة فتح وإعادة قفل تلقائي بعد دقيقة بنفس الأرقام — والصرف التجريبي اتلغى بعد التحقق
> عشان مايفضلش في سجل الخزينة الحقيقي. تفاصيل في تكملة 58.
>
> **🐛 وبعد كده فورًا، إصلاح:** owner سأل مباشرة بعد الشحن "التأخير بتاع الشهر الجديد بيتخصم
> من الشهر القديم؟؟" — الفحص أثبت إن الحساب المجمّد نفسه سليم 100% (لم يتغيّر خالص)، لكن كارت
> "السلف" في صفحة الموظف كان بيستخدم بالغلط نفس المتغيّر اللي قسم "الفترة الحالية" بيستخدمه،
> فلما فترة مقفولة معلّقة موجودة، قسم "الفترة الحالية (الشهر الجديد)" كان بيعرض بالغلط رقم
> الشهر القديم المقفول بدل إجمالي أيامه هو — عرض بحت، مش خلط حقيقي في الحساب. فصل المتغيّرين
> + تسمية واضحة "(الشهر المقفول)"/"(الفترة الحالية)" جنب كل رقم. تفاصيل في تكملة 59.
>
> **وبعد كده مباشرة، سؤال متابعة owner كشف فجوة حقيقية:** "دلوقتي هل سلف الشهر القديم ليها
> علاقة بالشهر الجديد؟ المفروض يتخصم تبع الشهر القديم مع بردو تأخير الشهر القديم والشهر
> الجديد ميبقاش عليه سلف" — تأكيد صريح: "السلفة بتتخصم تلقائي من المرتب وبتتكتب سلفه الموظف
> مش بيسددها إلا من مرتبه." قبل كده السلفة كانت رصيد جاري بيتخصم (في العرض بس) من متبقي كل
> شهر جديد لحد الأبد لحد ما تتسدد يدويًا. دلوقتي: أول "صرف مرتب" حقيقي على فترة مقفولة بيسجّل
> سداد `SALARY_DEDUCTION` تلقائي على السلف المستحقة (الأقدم فالأقدم، لحد سقف مرتب الفترة
> نفسها) — فتختفي من كل شهر جاي بعد كده. الموظفين الأسبوعيين وأي دفعة من غير فترة مقفولة
> فضلوا زي ما هم بالظبط. متحقق منه حي بالكامل، والصرف/السداد التجريبي اتلغيا بعد التحقق.
> تفاصيل في تكملة 60.
>
> **وبعد كده فورًا، 3 أسئلة متتالية كشفوا فجوتين حقيقيتين:** "متأخر كام ساعه بالظبط الشهر
> اللي فات" — النظام كان بيعرض بس *مبلغ* التسوية، صفر رقم بالساعات الفعلية؛ و"ليه مش ظاهرلي
> إجمالي الشهر الجديد لحد دلوقتي" — تقرير السلف والمرتبات كان بيعرض بس رقم الشهر المقفول لما
> يكون موجود، ورقم الشهر الحالي الجاري كان بيختفي تمامًا. الحل: "ساعات التأخير" (س/د حقيقية،
> مش دقايق خام ولا مبلغ) جنب كل شهر مقفول وجنب الفترة الحالية، و`EmployeeAdvanceSummary`
> بقى فيه `currentPeriodAdjustment` حي دايمًا (معلوماتي بحت، مالوش علاقة بـnetDue) — فتقرير
> السلف والمرتبات بقى يعرض "(الشهر المقفول)" و"(الشهر الحالي لحد دلوقتي)" مع بعض. تفاصيل في
> تكملة 61.
>
> **🔴 وبعد كده، إصلاح أمني حقيقي:** owner طلب "افصل أمين خزينة كليوباترا عن أمين خزينة
> برينتنج فا ميظهرش ده هنا ولا ده هنا" — الفحص كشف إن الفصل ده **مش موجود فعليًا** زي ما كان
> موثّق سابقًا، كان مجرد فلتر عرض اختياري بدون أي تحقق حقيقي في الـBackend. أي أمين خزينة
> (`CASHIER`، صلاحية `treasury.*`) كان يقدر يشوف/يعدّل/يحذف حركات خزينة **أي فرع تاني** بمجرد
> تغيير `branchId` في الطلب — وده شامل قراءة الرصيد، إنشاء/تعديل/حذف حركة، وحتى **تقفيل أو
> إعادة فتح يوم الفرع التاني فعليًا**. كل نقاط الدخول بقت بتتحقق فعليًا من
> `accessibleBranchIds` (نفس البنية المُختبرة أصلًا في مسارات السلف/المرتبات) بدل الثقة العمياء
> في أي `branchId` مُرسَل — Super Admin بس هو اللي يتخطى الفحص. تفاصيل في تكملة 62.
>
> **وبعد كده مباشرة (مقاطعة أثناء العمل)، طلب لوحة الإنتاج الكبير:** "عايز كل الطلبات في مكان
> واحد وفي فلتر... افلتر براحتي واقدر اغير في ترتيبه... لازم اشوف إسم الصنف مش رقم الفاتورة
> علشان اعرف هي ايه من بره." فحص أولي أكّد إن الـcheckbox/الفلاتر بالـquery params/اسم الصنف
> في تاب "الطلبات" كانوا موجودين بالفعل — المطلوب فعليًا: تاب "الكل" جديد جوه "الأقسام" يجمع كل
> الأقسام/المسارات مع بعض (فلتر مسار Client-side بعدين)، ترتيب يدوي (↑/↓، `StageInstance.
> manualSortOrder` عمود جديد، خاص بكل `stageId` على حدة — owner: "ترتيب خاص بكل قسم/مرحلة")،
> وعمود "الصنف" على تاب "الأقسام" كله (كان غايب منه، موجود بس في تاب "الطلبات"). فيو
> الكانبان-بحسب-الوركفلو اتفصل ومؤجل عمدًا بموافقة owner ("ملحق بعد ما الشاشة الموحدة تخلص").
> **🐛 اتصلح أثناء الاختبار الحي قبل الشحن:** فلتر المسار الجديد كان بيحط أي قسم مالوش
> `productionTrack` (خدمة العملاء/مورّد خارجي/التشطيب — مش بس التصميم) تحت "التصميم" غلط،
> فاختيار أي مسار كان بيطابق كل الصفوف ومايفلترش حاجة — الإصلاح: قسم غير موسوم بيبان بس تحت
> "كل الأقسام/المسارات"، مش أي فلتر محدد. متحقق منه حي بالكامل (فلتر المسار قبل/بعد الإصلاح،
> الترتيب اليدوي سواب واسترجاع، بحث اسم الصنف)، 107+6 اختبار فضلوا ناجحين. تفاصيل في تكملة 63.
>
> **وبعد كده مباشرة، دفعة كبيرة تانية (تكملة 64):** فيو الكانبان-بحسب-الوركفلو المؤجل من
> تكملة 63 اتبنى (تاب "حسب الوركفلو" — أعمدة = مراحل وركفلو مختار، كروت = نفس صفوف "الأقسام"
> مجمّعة بالمرحلة)؛ ترتيب Drag-and-Drop حقيقي (`@dnd-kit`) بدل زرارين ↑/↓ — owner: "عايز اقدر
> احرك الصفوف بأريحية شبه صفوف نوشن"؛ **🔴 إصلاح جوهري:** owner لاحظ صنفين مختلفين (MTSC:
> 1000 كارت اوفست + 300 كارت ديجيتال) واقعين في نفس أمر الشغل بدون أي رابط تصميم مشترك
> (`groupId` فاضي) — السبب: `tryAutoCreateWorkOrders` كانت بتجمّع الأصناف حسب
> `productionTrack` بس، فأي صنفين غير مرتبطين على نفس المسار كانوا بيتزنقوا مع بعض. المفتاح
> بقى `(productionTrack, groupId)` — صنف من غير `groupId` ياخد أمر شغل مستقل دايمًا، صنفين
> بـ`groupId` مشترك (تصميم واحد بمتغيرات إنتاج متعددة) لسه بيتجمّعوا صح. نطاق الإصلاح: مسار
> الإنشاء بس — مسار التعديل (`updateOrder`) اتسيب عمدًا (بيحافظ على استمرارية أمر الشغل عبر
> التعديلات بمطابقة المسار، تغييره كان هيكسرها). زائد زرار "تنزيل كل أوامر الشغل PDF" جديد
> (كل أمر شغل في صفحة كاملة لوحده) بعد اكتشاف إن `break-before-page` الأصلي مالوش أي تأثير
> على تصدير PDF خالص (بس على الطباعة الفعلية) — علامة جديدة `data-pdf-page-break-before`
> صحّحت المشكلتين مع بعض. متحقق منه حي بالكامل (كانبان، سحب-وإفلات، أوردر تجريبي حقيقي أنتج
> أمرين شغل منفصلين ثم اتمسح، PDF مجمّع حي). تفاصيل في تكملة 64.
>
> **وبعد كده مباشرة (تكملة 65):** تعديل مباشر جوّه الصف/الكارت (زي نوشن، عائلة `Editable*Cell`
> الموجودة بالفعل) للأولوية/تاريخ الاستحقاق/الموظف المسؤول/سبب الانتظار — بدون فتح Dialog؛
> تاب "مورّد خارجي" مستقل جوّه "الأقسام" (قسم حقيقي كان موجود بالفعل بس مدموج، owner: "اقدر
> اشوف الطلبات اللي عند مورد خارجي واقدر ادوس إنها تم استلامها ولا لا")؛ زرار "طباعة قائمة
> الاستلام" بيطبع اسم الصنف + اسم المورد + عنوانه؛ 🐛 إصلاح Hydration حقيقي (`dnd-kit`'s
> `DndContext` كان بيحط `<div>` مخفي جوّه `<tbody>` — مش مسموح في HTML — الإصلاح: لف
> `<Table>` كله مش `<TableBody>` بس)؛ و🔴 **ترحيل بيانات فعلي**: owner لاحظ إن أوردر MTSC
> القديم (قبل إصلاح تكملة 64 بستة أيام) لسه فيه أمرين شغل مجمّعين (2 صنف + 9 أصناف مختلفة
> تمامًا مع بعض!) — الفحص أكّد إن الاتنين لسه في أول مرحلة بالظبط (صفر تقدّم)، فاتعمل ترحيل
> آمن بنفس دالة `createWorkOrderForTrack` الموجودة يدويًا لكل صنف زيادة، فبقى الـ11 صنف = 11
> أمر شغل مستقل الآن، متحقق منه حي على شاشة الفاتورة ولوحة الإنتاج. تفاصيل في تكملة 65.
>
> **وبعد كده مباشرة (تكملة 66):** بحث بإسم الصنف زاد في تاب "الطلبات"؛ عمود "المورد" في جدول
> "الأقسام" (كل الأولوية له تاب "مورّد خارجي")؛ و🔴 **owner أصرّ "اتأكد من كده ضروري"** —
> تدقيق شامل لكل قاعدة البيانات (مش بس MTSC) كشف أمرين شغل تانيين لسه مجمّعين (شركة بلانت
> اكس: 3 أصناف، شركة كاليكس: صنفين) — الاتنين كانوا خطوا بالفعل خطوة كاملة (وصلوا "موافقة
> العميل"/"إرسال للمورد")، فالترحيل ده احتاج نسخ كل تاريخ الـStageInstance بالظبط (مش مجرد
> إعادة إنشاء من الأول زي MTSC) عشان الصنف المنفصل يكمل من نفس مكانه بالظبط. بعد الترحيل:
> صفر أمر شغل مجمّع فاضل في كل قاعدة البيانات (تدقيق مباشر تاني أكّد كده). ملاحظة جانبية:
> "ختم مقاس 13" اللي بان مكرر لعميل واحد اتضح إنه فعليًا أوردرين حقيقيين منفصلين (مش باگ) —
> طلب owner لمنطق توجيه جديد (فحص مخزون قبل المورد حسب مقاس الماكينة) لسه معلّق محتاج توضيح.
> تفاصيل في تكملة 66.
>
> **وبعد كده مباشرة (تكملة 67)، طلب سريع:** checkbox "إنهاء" بقى جنب اسم الصنف مباشرة في
> الثلاث فيوهات (جدول/كارت موبايل/كارت كانبان) بدل ما يكون بس في عمود "الإجراءات" الأخير —
> `useQueueActions`'s `actionButtons` اتقسّم لـ`completeCheckbox`/`secondaryActions` منفصلين.
>
> **وبعد كده (تكملة 68):** owner رجع لطلب "التحكم في مكان العمود" اللي كان متأجل جزء منه —
> تأكيد صريح إنه المقصود "الاتنين معا" (جدول الأقسام + كانبان حسب الوركفلو). ترتيب Drag-and-
> Drop حقيقي وتغيير عرض بالسحب لأعمدة الاتنين مع بعض، كتفضيل شخصي في `localStorage` (هوك جديد
> `useColumnLayout`) — جدول الأقسام اتحوّل لنموذج أعمدة مدفوع بالبيانات عشان الترتيب يبقى ممكن
> أصلًا، وكل وركفلو في الكانبان له تخطيط مستقل. 🐛 إصلاح حقيقي أثناء الاختبار: مقبض تغيير
> العرض كان نصه بيتقص بصمت بسبب `overflow: hidden` على العمود الأب، فبقى غير قابل للسحب إلا
> بدقة بكسل واحد تقريبًا — اتصلح بجعله بالكامل جوّه صندوق العمود. متحقق منه حي بالكامل
> (ترتيب + تغيير عرض، جدول وكانبان)، 107+6 اختبار فضلوا ناجحين.
>
> **🔴 وبعد كده مباشرة (تكملة 69)، إصلاح أمني تاني:** owner سأل "ليه معملش دورين... زي ما
> قولت" عن فصل خزينة الفرعين (تكملة 62) — بعد شرح إن الفصل بفرع مربوط بالموظف مش بدورين
> منفصلين بالاسم، owner كشف إن ويدجت الداشبورد "صافي الربح والخزينة بالفرع" لسه بتعرض الفرعين
> لأي حد عنده `reports.view` (ممنوحة كمان لدور SALES العادي) بلا فلترة خالص — إصلاح تكملة 62
> كان غطى شاشة الخزينة نفسها بس، مش الويدجت ده. نفس شكل الإصلاح بالحرف: `getCompanyFinancial
> Summary(branchIds?)` بقت تفلتر، والـcontroller بيمرر كل الفروع لـSuper Admin بس وإلا
> `accessibleBranchIds` بتاع المستخدم. متحقق منه ضد قاعدة البيانات الحية (فرع واحد يرجع أرقامه
> بس، بدون فلتر يرجع الفرعين + الإجمالي الصافي)، صفر تغيير في الواجهة، 107 اختبار فضلوا ناجحين.
>
> **وبعد كده مباشرة (تكملة 70)، طلبين:** (1) نفس جدول الفرعين+الإجمالي بقى ظاهر في شاشة الخزينة
> كمان، مش بس الداشبورد — مكوّن مشترك `BranchFinancialSummaryTable` بدل تكرار الكود. (2) 🔴
> owner سأل "ليه مقسم وورك فلو الأوفست واللوحات لأكتر من حاجه، المفروض وورك فلو واحد بيظهر فيه
> شغل كل القسم" — كانبان "حسب الوركفلو" (تكملة 64) كان بيوريك نسخة واحدة بس (v1/v2/v3) في كل
> مرة. فحص مباشر أثبت التكلفة الحقيقية: `READY_PRODUCTS` عندها 2 أمر شغل حي فعليًا على v1 و4 على
> v2 في نفس اللحظة — اختيار آخر نسخة بس كان هيخفي 2 من 6 حقيقيين. القائمة بقت تعرض كل "كود"
> وركفلو مرة واحدة (مش نسخة بنسخة)، وبتجيب شغل كل نسخه المنشورة مع بعض (`Promise.all`)، الأعمدة
> = كل أسماء مراحل مختلفة عبر كل النسخ (مترابطة بالاسم مش الـid، كل نسخة ليها ids مختلفة). 🐛
> إصلاح سباق (Race) مصاحب: تحميل واحد بقى عدة طلبات مع بعض، فتبديل سريع بين وركفلوهين ممكن
> يخلي دفعة قديمة تمسح بيانات دفعة أحدث — اتصلح بعلامة رقم طلب. متحقق منه حي بالكامل، 107+6
> اختبار فضلوا ناجحين.
>
> **وبعد كده (تكملة 71)، بموافقة owner صريحة (بيلمس التسعير، قاعدة 3):** "عايز اقدر اعدل على
> السعر النهائي لأي بند في الفاتورة لأي قسم" — تعديل جديد `finalPriceOverride` يستبدل الناتج
> النهائي المحسوب بالكامل، لكل الأنواع التسعة عدا "بند يدوي" (كان أقصى حاجة موجودة قبل كده
> تعديل سعر الوحدة/المتر للوحات/منتجات جاهزة/خدمات/مخزون بس — الأوفست/الدفاتر/الديجيتال مكنش
> فيهم أي استبدال للناتج النهائي خالص). اتنفّذ كـWrapper حوالين `computeItemPricing` من غير ما
> يتلمس أي معادلة قائمة (صفر خطر، قاعدة 3/4 محققة بالبنية)، قبل خصم البند الواحد % زي السعر
> العادي. متحقق منه حي بالكامل من طرف لطرف — بند حقيقي، حفظ فاتورة، **وإعادة فتحها للتعديل
> استرجعت القيمة الخام نفسها** (نفس فحص تكملة 51 بالحرف)، الفاتورة التجريبية اتمسحت بعدها.
> _[حدّث هذا السطر و`docs/AI/PROJECT_STATUS.md` باستمرار عشان أي جلسة Claude Code جديدة تعرف
> تبدأ منين]_

## ✅ القرار المعماري محسوم — Cloud/Online بالكامل

صاحب المشروع أكّد رسميًا: النظام هيفضل **Online بالكامل**، وهيتوفّر اتصال إنترنت مستقر للورشة.
معنى ده:
- مفيش Offline Mode ولا Sync ولا Conflict Resolution مطلوبين — النسخة المحلية/الأجهزة الثلاثة
  في الوثيقة الأصلية كانت افتراض قديم غير دقيق، اتحدّثت بالكامل (قسم 4 في المرجع الكامل).
  ما تبنيش أي حاجة على أساس "جهاز 3 هو الأصل" أو منطق Local-First بعد كده.
- مفيش QR على أمر الشغل — التتبع كله من على شاشات النظام مباشرة (قسم 6.4 المحدَّث).
- المسؤولية الوحيدة المتبقية: تأمين اتصال إنترنت مستقر للورشة (مش مسؤولية الكود)، والتأكد من
  Automated Backups مفعّلة على مستوى الاستضافة السحابية (Supabase).

---

## 6. الصلاحيات (RBAC) — الأدوار المرجعية

`Super Admin, Manager, Sales, Call Center, Marketing, POS, Pre-Press, Production, Finishing,
Warehouse, Finance, Designer, Developer`

مستويات الصلاحية لكل موديول: `View, Create, Edit, Delete, Approve, Cancel, Export, Financial Access`

> **الأدوار الفعلية المزروعة في السيستم حاليًا** (`apps/api/prisma/seed.ts`):
> `SUPER_ADMIN, ADMIN, SALES, CASHIER, PRODUCTION_MANAGER, DESIGNER, PRINTING_OPERATOR, VIEWER`
> — تغطي الاحتياج الحالي (8 موظفين) لكن مفيش أدوار مستقلة بعد لـ Call Center أو Marketing أو
> Finance (مغطاة حاليًا بصلاحيات ADMIN/SUPER_ADMIN العامة). نظام الصلاحيات نفسه Database-Driven
> بالكامل (ADR 0022) — مش Hardcoded — الموديولات المفعّلة فعليًا: `attendance, employees,
> inventory, orders, partners, permissions, quotations, reports, roles, settings, tenders,
> treasury, work-orders, workflow-templates`.

### ⭐ أولوية عليا: الوضوح + تعدد الأقسام للموظف الواحد (قسم 19.0 و21.1 بالمرجع الكامل)

الفريق صغير حاليًا والموظف الواحد بيمسك أكتر من قسم في نفس الوقت — العدد هيزيد تدريجيًا. اتنين
قواعد لازم تتطبق على أي شاشة إدارية أو Feature جديدة من هنا وطالع:

- **قاعدة حاسمة — ممنوع دمج الشاشات:** كل قسم/Module له شاشة مستقلة وثابتة الشكل تتبني مرة
  واحدة. تعيين موظف لأكتر من قسم معناه بس إنه هيشوف أكتر من رابط في قائمة التنقل، كل رابط بيوديه
  لنفس الشاشة المستقلة بتاعت القسم — **مش شاشة مدمجة أو مركّبة خاصة بيه لأنه بيمسك أكتر من قسم**.
  لما العدد يزيد لاحقًا وينفصل كل قسم لشخص مستقل، الشاشات تفضل زي ما هي بالظبط، وبس الصلاحيات
  بتتغيّر.
- **كل موظف ممكن ياخد أكتر من دور (Role) واحد في نفس الوقت** — شاشة إدارة الموظفين لازم تعرض
  بوضوح كل موظف وكل الأقسام المسندة له في مكان واحد، وإضافة/إزالة قسم تكون خطوة سريعة (Toggle)
  من غير ما تأثر على بياناته القديمة.
- **لوحة الإنتاج (قسم 6.5) هي الأولوية العليا في التصميم** — مفهومة من أول نظرة (ألوان/أيقونات
  واضحة للحالة، مش أرقام محتاجة تفسير)، أقل عدد ضغطات ممكن للوصول لأي Job، وتشتغل كويس على
  الموبايل والتابلت مش بس الشاشة الكبيرة.
- **معيار القبول لأي شاشة جديدة:** موظف بيمسك 2-3 أقسام يقدر يفهمها ويستخدمها من أول مرة من
  غير تدريب — وده معناه شاشات منفصلة وبسيطة لكل قسم، مش شاشة واحدة مجمّعة.

---

## 7. الحضور والانصراف + الباركود (إضافات جديدة)

### الحضور والانصراف (قسم 3.1 بالمرجع الكامل)

- إدارة الحضور/الانصراف بالكامل (عرض، تعديل، تقارير) **مقصورة على حساب المسؤول العام فقط** —
  استثناء مقصود من RBAC العام لحساسية بيانات الرواتب. **جاري تنفيذها دلوقتي** (تقييد الشاشة على
  `roleNames.includes('SUPER_ADMIN')` صريحة).
- **تسجيل الحضور اليومي بيتم من كشك برقم سري (PIN) مخصص لكل فرع** — الموظف بيكتب اسمه (كتابة
  حرة، مش اختيار من قائمة) والـPIN بتاعه على جهاز الكشك. **موجود فعلاً وشغال لفرع كليوباترا،
  جاري توسيعه دلوقتي ليغطي فرع بيت الطباعة كمان.** ده تصحيح على مسودة سابقة كانت مقترحة تستبدل
  الكشك بالكامل بـGPS Geofencing — القرار النهائي إن الكشك يفضل ويتوسع، مش يتلغى.
- **الـGPS (خريطة تفاعلية + تأكيد موقع تلقائي) بيُستخدم بس للمهام الخارجية** (Field Assignment) —
  مفهوم منفصل تمامًا عن حضور الفرع اليومي. المسؤول بيحدد مكان المهمة بخريطة تفاعلية (مش كتابة
  إحداثيات)، والموظف بيأكد وصوله أوتوماتيك بالـGPS الفعلي بتاع موبايله. **موجود بالفعل في الكود
  بدون تغيير مطلوب.**
- **تقييد الدخول للنظام حسب شيفت الموظف** (اتسألت صراحة 2026-08-20، لسه صفر تنفيذ) — الموظف
  ميقدرش يفتح النظام بره وقت شيفته المحدد إلا بإذن صريح من المسؤول العام. **محتاج قرارات عمل
  قبل أي كود:** إيه شكل "الشيفت" نفسه (وقت ثابت لكل موظف؟ جدول أسبوعي؟)، وإزاي المسؤول يديله
  إذن استثنائي (لمرة واحدة؟ لفترة؟)، وهل ده تقييد على تسجيل الدخول نفسه أم على كل Request بعد
  الدخول (لو الموظف كان داخل وقت الشيفت وفضل الشيفت خلص). مرتبط منطقيًا بجدول الحضور/الشيفتات
  الموجود، مش تقييد مستقل.

### الباركود وقسم المكتبة (قسم 12.5 بالمرجع الكامل)

- **تصحيح توثيقي (2026-08-26):** تبيّن إن ده **موجود ومكتمل بالفعل** (Commit `fd4b0c6`، قبل
  ما تتوثّق هنا صح) — مش "لسه مش موجود" زي ما كان مكتوب سابقًا. `InventoryItem.barcode` حقل
  اختياري (`String? @unique`) قابل للتعديل من شاشة المخزون، وتاب "بضاعة من المخزون" في شاشة
  إنشاء الطلب فيه حقل مسح باركود حقيقي (`GET /api/inventory-items/by-barcode/:barcode`) —
  الموظف يمسح/يكتب الكود + Enter فيتضاف الصنف للفاتورة فورًا (بسعره من `salePrice`)، مطابق
  تمامًا لمواصفة "جهاز القراءة كـHID + Enter". **متحقق منه حي** على صنف حقيقي (باركود `171`،
  "كتاب المعاصر انجليزى 2 ابتدائى ترم اول"، 189 ج.م). النطاق الحالي مقصور على `InventoryItem`
  بس (مش ReadyProduct/Service) — قرار owner صريح (2026-08-26)، مش نسيان. الباقي الوحيد المؤجل
  عمدًا: توليد باركود داخلي تلقائي للأصناف اللي مالهاش باركود مورد أصلي (owner لسه مسألوش
  عنه صراحة).

---

## 8. ملاحظات على التكاملات الخارجية

- WhatsApp / Social Media / Call Center integrations تعتمد بالكامل على الـ APIs الرسمية
  والأذونات المتاحة فعليًا. **لا تفترض** إن أي منصة بتسمح بكل عمليات القراءة/الرد/النشر
  بدون تكامل رسمي موثّق ومُفعّل.
- أي Integration خارجي = Module مستقل وقابل للاستبدال، منفصل عن الـ Core System.
- **لا يوجد أي تكامل خارجي حالي** (لا WhatsApp، لا Social، لا Call Center، لا SMS/Email
  للإشعارات) — كل ده لسه مرحلة مستقبلية بالكامل.

---

## 9. معلومات تقنية

- **اللغة/الـ Framework الأساسي:** TypeScript في كل مكان. الواجهة: React 19 + Vite + Tailwind
  CSS + مكوّنات shadcn/ui + React Router. الـ Backend: Express.js + TypeScript. مكتبة مشتركة
  (`packages/shared`) فيها كل Zod validation schemas + Pricing Engine + تعريف الصلاحيات،
  مستخدمة من الـ Frontend والـ Backend مع بعض (Monorepo عبر npm workspaces).
- **قاعدة البيانات:** PostgreSQL مُدارة عبر Supabase، الوصول ليها عن طريق Prisma ORM (v7).
  الـ Authentication عن طريق Supabase Auth (JWT)، والـ Authorization (RBAC) طبقة منفصلة فوقيها
  بالكامل Database-Driven (جداول StaffProfile/Role/Permission/RolePermission — انظر ADR 0022).
  قاعدة بيانات واحدة فقط، مشتركة بين التطوير المحلي والإنتاج (مفيش Staging/Local DB منفصلة).
- **الاستضافة:** سحابية بالكامل — Vercel (الواجهة) + Render (الـ API) + Supabase (قاعدة
  البيانات + Auth) — لا يوجد أي جزء محلي/Offline.
- **أوامر التشغيل/الاختبار (build/test/lint):** من جذر المشروع —
  `npm run build` (بيبني shared ثم api ثم web بالترتيب ده تحديدًا لأن الاتنين بيعتمدوا على shared)،
  `npm run dev:web` / `npm run dev:api` (تشغيل التطوير)، `npm run lint`، `npm run typecheck`،
  `npm run format`. أوامر Prisma: `npm run prisma:generate` و`npm run prisma:migrate`
  (لازم تتنفذ من جوه `apps/api` أو عن طريق الـ workspace flag — إعداد Prisma 7 بتاعنا
  cwd-relative، مش بيقرأ `--schema` من روت المشروع).
- **هيكل المجلدات الحالي:**
  - `apps/web` — الواجهة (React/Vite).
  - `apps/api` — الـ Backend (Express)، وفيه `prisma/schema.prisma` (مصدر الحقيقة لقاعدة
    البيانات) و`prisma/migrations/` (كل Migration بمجلد منفصل بتاريخه).
  - `packages/shared` — Zod schemas + Pricing Engine + تعريف الصلاحيات، مشترك بين web وapi.
  - `docs/AI/` — أرشيف تفصيلي لكل Feature اتبنت (`FEATURES/FEATURE-XXX/`)، بالإضافة لملفات
    مرجعية زي `PRICING_ENGINE_SPEC.md` و`VISION.md` و`MASTER_HANDOFF.md`.
  - `adr/` — Architecture Decision Records مرقمة (0001–0027 حاليًا) توثّق قرارات معمارية
    محددة (زي اختيار Prisma، Supabase Auth، الـ RBAC، ترقيم المستندات، ...).
  - جذر المشروع فيه كمان `ARCHITECTURE.md`، `LEGACY_ANALYSIS.md`، `LEGACY_MAPPING.md`،
    `CODING_STANDARDS.md`، `API_CONVENTIONS.md`، `MIGRATION_PLAN.md` — مراجع تكميلية موجودة
    من قبل، ومفيش تعارض بينها وبين هذا الملف؛ هذا الملف (`CLAUDE.md`) هو المرجع السريع اللي
    بيتقرأ أول كل جلسة، والباقي مراجع تفصيلية إضافية.
- **مكان الـ Pricing Engine الحالي في الكود:** `packages/shared/src/pricing/` — أربع ملفات
  حاليًا: `sizeCalculation.ts`، `costCalculation.ts`، `boardsCostCalculation.ts`،
  `productCostCalculation.ts` (+ `digitalCostCalculation.ts` جاري إضافتها دلوقتي لمسار
  الديجيتال). بينادى عليهم من `apps/api/src/services/pricingEngineService.ts`. التوثيق الكامل
  للمنطق في `docs/AI/PRICING_ENGINE_SPEC.md`. طبقًا لـ ADR-0016، منطق الحساب الحالي (غير
  الديجيتال) **منقول حرفيًا (verbatim port)** من النظام القديم (Legacy) — يعني القاعدة رقم 3+4
  هنا (ممنوع تغيير Pricing/حساب الدفاتر والورق والتراج والهالك بدون موافقة صريحة) بتتطبق عليه
  بالحرف، ومفيش أي استثناء.
- **تكلفة المورد الحقيقية منفصلة عمدًا عن أرقام معادلة التسعير** (2026-08-27، owner أكّد صراحة
  إن `zincPrice`/`SheetType.price`/أسعار البورد الحالية مُقدَّرة بزيادة بسيطة فوق سعر المورد
  الحقيقي، مش نفس الرقم) — `Setting.zincSupplierCost`، `SheetType.costPrice`،
  `Setting.boardsXxxSupplierCost` كلهم حقول تكلفة إضافية بس، **بتتغذّى تقارير الربح فقط
  (`branchFinancialsService.ts`) ومتلمسش معادلة التسعير نفسها خالص** — لو أي جزء من التكلفة
  الحقيقية ناقص، الكود يرجع تلقائيًا لنفس التقدير القديم (نسبة الهامش)، صفر تراجع في أي رقم
  كان شغال قبل كده.

---

## 10. مرجع إضافي

الوثيقة الكاملة (`system_specifications_v2.md`) فيها تفاصيل أوسع لكل قسم أعلاه — خصوصًا:
Unified Inbox (قسم 8)، Call Center Dashboard (قسم 9)، Costing & Pricing (قسم 13)،
Notifications Engine (قسم 18)، وUI/UX لكل شاشة (قسم 19). راجعها عند العمل على تفاصيل أي قسم بعينه.

تقرير الفجوات الكامل (كل قسم من الـ25 مقابل حالته الفعلية في الكود، أول 3-5 Features مقترحة
للبدء بيها): `docs/gap_analysis_report.md`.
